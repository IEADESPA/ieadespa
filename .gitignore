node_modules/
api/node_modules/
api/local.settings.json
.env

# Segredos de DEV (SOPS + Age): a chave PRIVADA nunca entra no repo. O arquivo
# CRIPTOGRAFADO (api/local.settings.enc.json) É versionado de propósito.
sops-age-key.txt
*.agekey
age-key.txt
keys.txt

# Logs do func start / Static Web Apps CLI (não versionar)
*.log
api/func.log
swa.log
swa2.log

# Banco mock local — não é banco de verdade, só desenvolvimento
api/data/mockdb.json

# Material de referência conceitual (protótipos externos usados como ideia
# pra FASE 5/6 — ver README) — nunca vira código do repo, e o .rar da EBD
# sozinho já passa de 400MB.
/relatorios-departamentos.rar
/relatorios-departamentos/
/chamada-ebd.rar
/chamada-ebd/
