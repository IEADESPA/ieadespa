{
  "name": "governanca-ieadespa-api",
  "version": "1.0.0",
  "description": "Backend de governança (CLI, CEI, NIF, AFM) - Azure Functions",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "func start",
    "test": "jest",
    "secrets:decrypt": "sops -d --output local.settings.json local.settings.enc.json",
    "secrets:encrypt": "sops -e --input-type json --output-type json --output local.settings.enc.json local.settings.json"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.33.0",
    "mssql": "^12.7.0"
  },
  "devDependencies": {
    "jest": "^30.5.1"
  }
}
